
# Redirect all routes to index.html for SPA routing
/* /index.html 200

# Proper MIME type handling for JavaScript modules
/*.js /index.html 200!
/*.js.map /index.html 200!

# Ensure static assets are served directly
/assets/* /assets/:splat 200
/website-uploads/* /website-uploads/:splat 200
